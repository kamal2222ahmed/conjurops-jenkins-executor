---
driver_plugin: docker

driver_config:
  socket: <%= ENV['DOCKER_HOST'] || 'unix:///var/run/docker.sock' %>

provisioner:
  name: chef_zero

platforms:
- name: ubuntu-14.04

suites:
  - name: master
    run_list:
      - recipe[conjurops-jenkins::master]
    attributes:
